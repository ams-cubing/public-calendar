{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["WCA_CLIENT_ID", "WCA_CLIENT_SECRET", "NEXTAUTH_SECRET", "DATABASE_URL", "NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
